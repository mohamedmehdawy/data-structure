print_inorder_expression:
the main idea is add () for each subtree and not just print want to return the prev print to make
add previous of it any paranthese
note:
use more test cases and show how can handle it
#
inorder(current, paranthese="", direction="left"): this function walk throw the tree in print it with paranthese
$
parameters:
    current: the current node
    direction: the place of the paranthese is in left or right
$
if no root, return no thing
call inorder for left with direction left
if the current operator and not a root:
-
print current with correct direction by check it
-
else:
-
print current
-
call inorder for right with with direction right

#