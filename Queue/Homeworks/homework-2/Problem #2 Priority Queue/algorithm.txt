Priority Queue => the idea is add the elements to the queue insted of his Priority
%%%%%%%%%%%%%%%%%%%%%%%%%%
edit in linked list queue => add Priority to the node
%%%%%%%%%%%%%%%%%%%%%%%%%%
properties:
- queue => the linked list queue

methods:
- enqueue => add the element to the queue insted of his Priority
#
if no elements append to the queue
if the Priority of current node is greater than the head, push first
else push end
#

- deqeue => remove the first element of the queue
#
call function remove the first element of the queue
#

- repr => display the queue with Priority
#
set current Priority = the first element Priority
loop until end
if each element Priority != the current Priority, we end the all of these Priority and print in new line
- and set the current Priority to the element Priority
#
